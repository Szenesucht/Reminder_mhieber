Starten mit: docker-compose up
(in entsprechendem Verzeichnis...)

Beenden am besten direkt mit docker-compose down -v
--> Damit werden immer auch direkt die erstellten files (unter anderem die DB) gelöscht. Vor allem wichtig bei Änderungen an DB

